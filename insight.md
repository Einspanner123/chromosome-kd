## 二、RF 理论可深化方向
### 方向 R1：直线度诊断指标ηstr​（理论 + 零代码）定义η str ( n ) ​ = ∥ D 1 ( n ) ​ ∥/∥ x 0 ( n ) ​ ∥ ，从 DPM-Solver++ 的校正项直接读出。理论上：- η str ​ → 0 ⇔ 学习轨迹为直线 ⇔ RF 训练成功- η str ​ 大 ⇔ 需要 reflow (2-RectFlow) 或更多步数价值 ：为"RF 在 2 步收敛"提供定量解释（不只是经验观察），并给出"何时需要 reflow"的可操作判据。可在论文 §5.3 补 1 段，无新实验。
### 方向 R2：RF 的 SNR 与最优步数分配（理论 + 少量实验）当前 SHTS 是启发式 SNR 网格。对 RF 路径 x t ​ = ( 1 − t ) x 0 ​ + tε ，SNR( t ) = ( 1 − t ) 2 / t 2 。在 v_θ 非恒定（学习残差）下，最优步数分配应使每步局部截断误差相等——可推导出 t n ∗ ​ 的解析条件。预期结论：等 SNR 间距（log-SNR 空间）接近最优，证明 SHTS 的合理性而非仅经验。风险 ：与已证伪的 IO1（adaptive step）有重叠，但 R2 是理论分析而非自适应提前终止，不触发 IO1 的失败模式。
### 方向 R3：x0-prediction vs v-prediction 在低维的等价性（纯理论）在 d = 4 且 RF 线性路径下， v = ( x t ​ − x 0 ​ ) / t 与x 0 ​ 仅差标量  t ，故网络输出空间等价。但梯度动态不同：v-prediction 的损失∥ v θ ​ − v ∥ 2 在 t → 0 时数值稳定（v 有界），而 x0-prediction 在 t → 1 时稳定。可分析哪个端点对染色体检测（框集中在 t ∈ [ 0 , 0.5 ] 训练信号更强）更优。价值 ：解释为何当前 x0-prediction + DPM-Solver++ 已经够好，预防审稿人质疑"为何不用 v-prediction"。
## 三、DPM-Solver++ 理论可深化方向
### 方向 D1：t 空间 vs log-SNR 空间插值（理论 + 实验）当前在t 空间多项式插值。VP-SDE 的 DPM-Solver++ 在  λ = lo g SNR 空间插值，因 λ 对应 ODE 的自然参数化。对 RF， λ ( t ) = 2 lo g (( 1 − t ) / t ) ，可推导：在λ 空间插值等价于在  t 空间用  ( 1 − t ) − 1 的非线性变换——对 t → 0 的奇点处理更自然。实验 ：替换 rectified_flow.py 中 step 方法的插值空间，3-seed 验证。预期收益小（+0.001-0.002 mAP）但理论一致性提升。
### 方向 D2：2M vs 2S 变体消融（实验 + 理论）当前是 2M（multistep，用历史 x0）。2S（singlestep，每步独立）在前几步更精确（不依赖冷启动历史），但失去后几步的信息复用。在 4 步设置下：- 2M：step 0 退化为 Euler，step 1-3 用历史- 2S：每步都 2 NFE，但更稳定风险 ：2S 总 NFE 翻倍（4→8），与 Heun 同 NFE，可能不会胜出。但作为消融可揭示 2M 的冷启动问题。
### 方向 D3：与 box_renewal 的理论矛盾（理论 + 实验）box_renewal 在每步后重置低置信框为噪声，破坏 x0_history 连续性。当前代码在 Top-K pruning 后 reset dpm_solver（ head.py:629 ），但 box_renewal 后未 reset。可分析：- box_renewal 后 D1 项的统计分布（应该接近 0，因 x0_pred 对 renewal 框无意义）- 是否应在 box_renewal 后也 reset？或禁用 box_renewal 仅在 step 0？价值 ：解释 Top-K K=100 为何掉点 -0.013——不只是"框少了"，还有"DPM-Solver++ 历史被破坏"。
## 四、模型结构与 solver 交互方向
### 方向 S1：cascade head 作为 implicit solver（纯理论）形式化分析：每个 cascade head 做 RoIAlign + DynamicConv + 预测 x0，本质上是"在固定 t 下对 x_t 的精化"。6 个 head 顺序执行，等价于"在 t 不变时对 x_t 的迭代收敛"。这给出了 cascade head × solver step 的解耦：- cascade head：固定 t，精化 x_t（"横向"收敛）- solver step：固定 x_t 精化链，推进 t（"纵向"推进）价值 ：解释为何 6 head × 4 step = 24 NFE 但 DPM-Solver++ 仅需 4 NFE 框架就够——因为 cascade head 在每个 t 内已经收敛。可在 §3.2 补 1 段。
### 方向 S2：PHTR（方向2）消融（实验）代码已有 head_time_scale / head_time_shift 但未消融。理论预期：让后期 head（head 4-6）在不同 t 上有不同行为，应改善"早期 t 探索 / 晚期 t 精化"的分工。风险 ：与 AdaLN-Zero 单独贡献为零（Appendix B）的结论一致，可能也是 +0.000 mAP。但 PHTR 是"head 间差异化"，AdaLN-Zero 是"时间条件化"，两者正交。
### 方向 S3：cascade head 数量 vs solver step 数权衡（实验 + 理论）当前 6 head × 4 step。理论上 DPM-Solver++ 二阶比 cascade head 一阶更高效，可尝试：- 3 head × 8 step（DPM-Solver++ 8 NFE）：总 NFE 24 不变，solver 阶数更高- 6 head × 2 step：测试 cascade head 能否补偿步数减少风险高 ：与已证伪的 N_cascade e2e（-0.172 mAP）部分重叠，但 S3 是"在已训模型上推理时调整"而非重训，风险较低。需重训新配置。