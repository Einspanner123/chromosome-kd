# LDMDet 实践方案：染色体目标检测进化之路

## 1. 现状分析：我们站在哪里？

目前 [LDMDet](projects/LDMDet/model.py) 已经基于 MMDetection 3.x 实现了 **DiffusionDet** 的核心框架，并初步引入了 **Rectified Flow (RF)**。

### 1.1 现有优势
- **生成式范式：** 相比于传统的一次性检测器，LDMDet 通过迭代去噪，在处理重叠严重的染色体目标时具有天然的潜力。
- **模块化设计：** 已经实现了 [rectified_flow.py](projects/LDMDet/mods/rectified_flow.py) 等独立组件，便于扩展。
- **采样效率：** 引入 RF 后，在同等采样步数下精度更高。DiffusionDet 原作在推理时默认使用 **1 步**（或为了更高精度使用 4-8 步），已初步具备实用价值。

### 1.2 改进空间
- **采样调度不精细：** 目前的 RF 采样使用的是均匀时间分布，而目标检测任务在 $t \to 1$（纯噪声）和 $t \to 0$（纯数据）阶段的学习难度不一。
- **缺乏全局建模：** 目前的检测头（Head）仍以卷积为主，对于染色体这种需要全局上下文（如同源染色体配对、数量约束）的任务，建模能力略显不足。
- **形状约束缺失：** 染色体是典型的细长物体，目前的水平 BBox (HBB) 无法很好地描述其旋转角度和重叠细节。

---

## 2. 技术升级路线图 (Roadmap)

我们将分三个阶段对 LDMDet 进行全面升级：

### 阶段一：范式升级 (Flow Matching + Logit-Normal)
- **目标：** 进一步提升直线路径的训练稳定性和采样精度。
- **动作：**
    - 引入 **Logit-Normal Sampling**：针对目标检测任务，将训练时的 $t$ 采样从均匀分布改为 Logit-Normal 分布，使模型更关注中间信噪比区域。
    - 优化 `rectified_flow.py`，支持更灵活的步长调度（如 Shifted Schedule）。

### 阶段二：架构进化 (DiT-Neck / Transformer Head)
- **目标：** 引入 Transformer 增强全局感知。
- **动作：**
    - 设计 **DiT-Neck**：在 Backbone 输出的特征图上，使用 Transformer Blocks 进行全局增强，并使用 `adaLN-Zero` 注入扩散步信息。
    - 考虑将 `diffusiondet_head` 的主干替换为轻量级 Transformer，提高对细长目标的注意力分配。

### 阶段三：极致提速 (Consistency Distillation)
- **目标：** 实现 1 步出图（Real-time Inference）。
- **动作：**
    - 对训练好的 Flow Matching 模型进行**一致性蒸馏 (LCM)**。
    - 最终目标是达到与 YOLO/RTMDet 相当的推理速度，同时保持生成式模型的高精度。

---

## 3. 针对染色体任务的垂直优化

### 3.1 引入旋转框 (OBB)
- **理由**：染色体在载玻片上的角度是随机的。水平框会包含大量多余背景和重叠。
- **方案**：修改 LDMDet，使其预测 $[x, y, w, h, \theta]$，并配合旋转 IoU Loss。
- **标注受限（仅有水平框）时的应对方案**：
    - **自动伪 OBB 生成**：在预处理阶段，利用 OpenCV 的 `minAreaRect` 从水平框内的染色体掩码（通过简单的 Otsu 阈值或形态学处理提取）中自动推断旋转角度。
    - **弱监督 BoxInst 方案**：不显式预测角度，而是引入弱监督的实例分割分支，仅利用水平框标注学习染色体的形状掩码，从而间接处理重叠问题。
    - **椭圆/关键点抽象**：将染色体建模为带有方向性的椭圆或中心线关键点，降低对精确 $\theta$ 标注的依赖。

### 3.2 数量先验与全局一致性约束
- **理由**：正常人类中期细胞具有固定的染色体数量（通常为 46 条）。传统的检测器将每个目标视为独立的，容易导致计数错误。
- **方案（由浅入深）**：
    - **全局计数分支 (Global Counting Branch)**：在 Neck 部分增加一个全局池化层，接 MLP 预测全图目标总数。将该预测值作为辅助 Loss（L1 或 Poisson Loss），迫使模型感知整体密度。
    - **期望和约束 (Sum-of-Probability Loss)**：强制所有检测框的分类概率之和（$\sum p_i$）趋近于全局预测数量或常数 46。这能有效抑制过检测（False Positives）。
    - **软排名损失 (Soft-TopK Loss)**：利用可微排名算子，鼓励置信度前 46 名的候选框与后排候选框之间产生明显的“分水岭”，增强模型在边缘情况下的决策果断性。
    - **动态阈值调制**：在推理阶段，不使用固定的 0.5 阈值，而是根据全局计数分支预测的数量，动态选取 Top-N 个结果，提高系统对不同质量图像的鲁棒性。

### 3.3 自动核型布局 (Karyotype Layout)
- **理由：** 检测只是第一步，最终需要按对排列。
- **方案：** 利用 DiT 的全局注意力，训练一个额外的配对分支（Pairing Branch），在检测的同时输出染色体的编号和配对关系。

---

## 4. 最终目标

1.  **高精度检测**：在复杂、重叠、拥挤的染色体切片上实现 $mAP_{50} > 0.95$。
2.  **严格数量一致性**：模型输出结果在 90% 以上的样本中满足 $N=46$ 的约束，无需人工二次计数。
3.  **弱监督落地**：在只有水平框标注的情况下，通过本方案实现接近有旋转框标注的检测精度。

---

## 5. 下一步执行路径 (Phase 2: 微观精细化)

### 5.1 伪旋转框 (Pseudo OBB) 自动生成
针对仅有 Mask 或 HBB 标注的数据，实现弱监督旋转框挖掘：
1. **Mask-to-OBB**：利用 OpenCV `minAreaRect` 从实例分割掩码中提取最小外接矩形。
2. **Angle Inference**：对于仅有水平框的目标，利用 LDMDet 的多级 Head 特征回归角度参数 $\theta$。

### 5.2 难例类别 (G组/Y) 专项增强
针对小目标崩溃问题：
1. **Loss Re-weighting**：根据类别频率和当前 mAP 表现，动态调整 Focal Loss 的类别权重。
2. **Feature Crop & Zoom**：在 Head 迭代中，对小目标候选框进行局部特征放大，补偿扩散过程中的信息丢失。
