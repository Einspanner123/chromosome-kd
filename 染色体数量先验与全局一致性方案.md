# 染色体数量先验与全局一致性方案

## 1. 核心背景与动机

染色体检测与通用目标检测（如 COCO 数据集）最大的区别在于其**极强的领域先验**：

- **固定的预期数量**：人类中期细胞通常包含 46 条染色体。
- **高密度与重叠**：目标高度聚集，局部特征极其相似，导致传统 NMS 极易失效。

本方案旨在通过引入**全局一致性约束**，将“计数”这一高层语义信息融入检测器的训练与推理过程中。

______________________________________________________________________

## 2. 方案一：全局计数辅助分支 (Global Counting Branch)

### 2.1 架构设计

在 Neck（如 FPN/PAFPN）的最高层特征图输出后，挂载一个轻量级的计数头：

1. **Global Pooling**：将 $H \\times W \\times C$ 的特征图池化为 $1 \\times 1 \\times C$。
2. **MLP Regressor**：两层全连接层，输出一个标量 $\\hat{N}$，代表模型预测的全图目标总数。

### 2.2 损失函数

使用 **Poisson Loss**（适用于计数任务）或简单的 **L1 Loss**：
$$L\_{count} = | \\hat{N} - N\_{gt} |\_1$$
该分支仅在训练时提供辅助监督，帮助 Backbone 学习全局密度特征。

______________________________________________________________________

## 3. 方案二：概率期望约束 (Sum-of-Probability Loss)

这是将数量先验转化为梯度信号的核心手段。

### 3.1 核心公式

假设模型输出 $M$ 个候选框（Proposals），每个框的分类概率为 $p_i$。我们期望所有概率之和趋近于真实数量 $N\_{gt}$：
$$L\_{sop} = \\left( \\sum\_{i=1}^{M} p_i - N\_{gt} \\right)^2$$

### 3.2 优化进阶：Top-K 降噪

在实际应用中，由于背景提案（Proposals）数量巨大（如 500 个），背景噪声的累积会严重干扰 $L\_{sop}$。

- **策略**：仅对概率最高的前 $K$ 个提案计算 SOP Loss（如 $K=64$）。
- **动态退火**：随训练进程将 $K$ 从 100 逐渐收缩至 54，引导模型从“粗略计数”转向“精准校准”。

______________________________________________________________________

## 4. 方案三：软排名损失 (Soft-TopK Loss / Margin Loss)

### 4.1 动机

我们希望模型不仅能预测对，还要“果断”。即前 46 名和第 47 名之间要有巨大的分水岭。

### 4.2 平滑 Margin 策略

利用排序后的得分差异施加约束：
$$L\_{margin} = (\\max(0, m - (S\_{46} - S\_{47})))^2$$

- **平方项优化**：使用平方 Margin 损失代替线性损失，使得在 Gap 接近达标时梯度更平滑，避免干扰主检测任务的回归精度。
- **动态阈值**：预设间隔 $m$ 通常取 0.3，强制拉开真值与背景的置信度鸿沟。

______________________________________________________________________

## 5. 最佳实践方案：四阶段温和启动与多级监督

针对 Rectified Flow 架构（如 LDMDet），单级监督会导致迭代链条断裂。必须采用平滑过渡方案。

### 5.1 多级监督平滑 (Supervision Smoothing)

不再仅对最后一级 Head 施加约束，而是对最后 3 级（$h_3, h_4, h_5$）同时进行一致性监督，确保梯度在迭代过程中平稳回传。

### 5.2 四阶段训练调度 (The 4-Phase Schedule)

| 阶段                  | 轮次 (示例) | 策略内容                                              | 目的                                          |
| :-------------------- | :---------- | :---------------------------------------------------- | :-------------------------------------------- |
| **Phase 1: 纯检测**   | 0 - 15      | 关闭所有一致性 Loss                                   | 让模型扎实掌握基础定位能力                    |
| **Phase 2: 密度感知** | 15 - 40     | 开启 $L\_{count}$，开启 Top-K 预热                    | 建立全局数量感知，不干扰分类                  |
| **Phase 3: 强约束**   | 40 - 160    | 全力开启 $L\_{sop}$ 与 $L\_{margin}$，执行 Top-K 退火 | 建立数量一致性逻辑，拉开分水岭                |
| **Phase 4: 冷却收割** | 160 - 200   | **一致性权重减半**，锁定 Top-K 范围                   | 消除强先验对边界框回归的副作用，冲刺 mAP 峰值 |

______________________________________________________________________

## 6. 核心代码实现

```python
# 核心逻辑摘录自 consistency_loss.py
probs = pred_logits.sigmoid().max(dim=-1)[0]
# Top-K 过滤与 SOP 计算
topk_probs, _ = torch.topk(probs, k=min(self.sop_topk, probs.shape[1]), dim=-1)
sum_probs = topk_probs.sum(dim=-1)
loss_sop = F.l1_loss(sum_probs, gt_counts.float())

# 平滑 Margin 损失
sorted_probs, _ = torch.sort(probs, dim=-1, descending=True)
k = int(gt_counts[i].item())
margin_gap = self.margin - (sorted_probs[i, k - 1] - sorted_probs[i, k])
loss_margin = torch.pow(F.relu(margin_gap), 2)
```

______________________________________________________________________

## 7. 最终效果 (LDMDet + Consistency)

- **mAP@50**: 达到 **0.941**，漏检率极低。
- **逻辑正确性**: 最后一级 Head 的分类损失显著低于中间层，解决了 Diffusion 迭代中的逻辑反弹问题。
- **业务价值**: 模型输出原生具备 46 条约束，无需复杂的后处理脚本即可直接用于核型分析。
